import type { ColDef } from "ag-grid-community";

export const defaultColumn: ColDef = {
    sortable: true,
    filter: false,
    floatingFilter: false,
    unSortIcon: true,
    resizable: true,
    flex: 1,
    minWidth: 150,
};
